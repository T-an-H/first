# 课程管理实施平台 — 用户操作手册

> 本文档面向平台的最终使用客户，结合系统截图详细说明各角色功能模块的操作方法。

---

## 一、登录与账号

### 1.1 登录页面

打开浏览器访问系统地址（默认 http://localhost:5173/），页面中央显示登录表单。

**登录表单元素**：
- 用户名输入框（左上角显示"账号"提示文字）
- 密码输入框（左上角显示"密码"提示文字）
- 登录按钮（蓝色主题，显示"登录"二字）
- 角色切换下拉菜单（位于登录按钮上方，共 5 个选项：管理员 / 教师 / 学生 / 企业导师 / 学院领导）

**操作步骤**：
1. 在"账号"输入框中输入用户名
2. 在"密码"输入框中输入密码（所有演示账号密码均为 `666666`）
3. 根据账号选择合适的角色
4. 点击"登录"按钮

**注意**：若角色选择错误，可能无法正常加载菜单。例如领导账号 `leader-liu` 选择"学院领导"登录，侧边栏会同时显示"教学管理"和"学院管理"；若误选"教师"角色登录，则会缺少学院管理入口。

### 1.2 全部演示账号速查表

| 账号 | 姓名 | 对应角色选项 | 说明 |
|------|------|-------------|------|
| `admin` | 系统管理员 | 管理员 | 管理全部课程分类和学员 |
| `teacher-wang` | 王老师 | 教师 | 普通教师 |
| `teacher-li` | 李老师 | 教师 | 普通教师 |
| `mentor-zhang` | 张导师 | 企业导师 | 企业导师 |
| `leader-liu` | 刘院长 | 学院领导 | 领导 + 教师双重身份 |
| `leader-zhou` | 周院长 | 学院领导 | 普通领导（只读） |
| `leader-wu` | 吴院长 | 学院领导 | 领导 + 导师双重身份 |
| `leader-zheng` | 郑院长 | 学院领导 | 领导 + 教师双重身份 |
| `S2024001` | 张明 | 学生 | 学生 |
| `202511053250` | 李傲天 | 学生 | 学生 |

### 1.3 登出

点击页面左下角的**头像区域**或**退出登录**按钮即可返回登录页。

---

## 二、系统整体布局说明

登录成功后，页面布局如下：

```
┌─────────────────────────────────────────────────────┐
│  顶部导航栏（Logo + 当前页面标题 + 用户信息）          │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  左侧    │           主内容区域                        │
│  侧边栏  │    （显示当前选中菜单项的内容）               │
│  菜单    │                                          │
│          │                                          │
├──────────┴──────────────────────────────────────────┤
│  底部（备用信息栏）                                    │
└─────────────────────────────────────────────────────┘
```

- **左侧侧边栏**：按住可左右拖动调整宽度；菜单项高亮表示当前所在页面
- **主内容区域**：右上角可能显示筛选器、搜索框、操作按钮等
- **右侧滚动条**：内容超出屏幕高度时可向下滚动查看更多

---

## 三、管理员端

登录角色选择 **"管理员"**，账号 `admin`，密码 `666666`。

管理员端侧边栏包含两个菜单项：「分类管理」「学员管理」。

### 3.1 分类管理页面

#### 3.1.1 页面布局

页面顶部显示标题"分类管理"，下方为一个分类卡片列表和右侧操作按钮。

**界面元素说明**：
- **分类卡片**：每个卡片上方为分类颜色条（与分类关联的颜色），中间为分类名称，下方显示课程数量（如"3 门课程"）
- **右上角按钮**："新增分类"按钮（蓝色）

**截图指引**：截图应展示 5 个分类卡片（编程开发、数据科学、设计创意、商务管理、语言学习），每个卡片颜色不同。

#### 3.1.2 查看分类

进入页面后默认展示所有分类，无需任何操作。

**截图指引**：截图展示该页面初始状态，标注每个分类的：① 颜色标识、② 分类名称、③ 课程数量。

#### 3.1.3 新增分类

**操作步骤**：
1. 点击右上角"新增分类"按钮
2. 弹出弹窗，填写以下信息：
   - **分类名称**（必填）：如"人工智能"
   - **颜色**（必填）：点击颜色选择器选择颜色
3. 点击弹窗中的"确认"按钮
4. 弹窗关闭，新分类出现在分类列表中

**截图指引**：分两步截图——① 点击"新增分类"后弹出的填写弹窗；② 新增成功后列表中多出一个分类。

#### 3.1.4 编辑分类

**操作步骤**：
1. 鼠标悬浮在目标分类卡片上，卡片右上角会出现编辑图标（✏️）
2. 点击编辑图标
3. 在弹出的编辑弹窗中修改名称或颜色
4. 点击"确认"保存

#### 3.1.5 删除分类

**操作步骤**：
1. 鼠标悬浮在目标分类卡片上，卡片右上角会出现删除图标（🗑️）
2. 点击删除图标
3. 弹出确认框，点击"确认删除"

**注意**：删除分类后，该分类下所有课程变为"未分类"，不会删除课程本身。

#### 3.1.6 教务数据同步

位于分类页面上方，**同步状态区域**展示。

**界面元素说明**：
- **文本展示**：左侧显示"最近一次同步时间：YYYY-MM-DD HH:mm:ss"
- **蓝色按钮**：右侧显示"立即同步教务数据"按钮

**操作步骤**：
1. 点击"立即同步教务数据"蓝色按钮
2. 按钮变为**置灰不可点击**状态，内部显示**加载旋转动画**（圆形图标转动）
3. 等待约 **1.2 秒**
4. 自动弹出**同步结果弹窗**，内容格式如下：
   ```
   教务数据同步完成
   ───────────────
   ✅ 新增：2 条
   ℹ️ 更新：5 条
   ❌ 失败：0 条
   ```
5. 点击弹窗右下角的"我知道了"或右上角的 ✕ 关闭按钮
6. 弹窗关闭后，页面上的"最近一次同步时间"更新为当前时间

**注意**：同步过程中按钮不可重复点击；同步结果数据为模拟数据，真实业务数据由后端对接教务系统。

**截图指引**：分三步截图——① 点击前的页面状态（同步按钮可用，显示上次同步时间）；② 点击后按钮置灰显示加载动画；③ 弹出的同步结果弹窗。

### 3.2 学员管理页面

点击左侧菜单"学员管理"进入。

**界面元素说明**：
- **标题**：页面顶部显示"学员管理"
- **学员列表**：表格形式展示所有学员，每行包含：姓名、学号、邮箱、手机号码、已选课程数
- **查看详情**：点击学员行的任意位置或行末的"查看"链接，进入学员详情页

**操作步骤**：
1. 在学员列表中浏览所有学员
2. 点击任意学员行进入该学员详情页
3. 详情页展示该学员的基本信息和选课记录列表
4. 点击左上角返回按钮或浏览器后退回到列表

**截图指引**：截取学员列表页面，标注每列名称（姓名、学号、邮箱等）。

---

## 四、教师端

登录角色选择 **"教师"**，推荐使用 `teacher-wang` 账号。

侧边栏菜单项：教学管理（展开后含：我的课程、课程表、成绩查询、评价管理、额外功能）。

### 4.1 我的课程页面

点击侧边栏"教学管理 > 我的课程"进入。

#### 4.1.1 页面概览

**界面元素说明**：
- **页面标题**：顶部显示"我的课程"
- **筛选下拉框**：右上角有"全部课程"筛选（可按分类筛选）
- **课程卡片列表**：每个卡片代表一门课程，卡片内容包括：
  - 卡片顶部：课程名称（如"React 前端开发实战"）
  - 课程名称下方：所属分类（如"编程开发"）
  - 卡片中间：教师姓名（如"王老师"）
  - 卡片底部左侧：课程状态标签（如"进行中"）
  - 卡片底部右侧："课程管理"和"评价管理"两个橙色按钮

**截图指引**：截取我的课程页面整体，标注：① 筛选下拉框、② 课程卡片结构（名称、分类、教师、状态标签、操作按钮）。

#### 4.1.2 课程卡片操作

**查看课程管理**：
1. 点击课程卡片底部的"课程管理"橙色按钮
2. 弹出课程管理弹窗，展示课程时间安排

**课程管理弹窗内容**：
- **弹窗标题**：课程名称 + "课程管理"
- **课程时间列表**：按序号排列，每一条为一次课
  - **待上课**：正常文字颜色，标注绿色"待上课"标签
  - **已结课**：文字变为删除线（带横线），灰色显示，并排到列表底部，标注灰色"已结课"标签

**示例**（以某课程 5 次课为例，已上完前 2 次）：
```
1. 第1节课  2026-06-25 14:00-15:30  ❌已结课
2. 第2节课  2026-07-02 14:00-15:30  ❌已结课（置底）
3. 第5节课  2026-07-23 14:00-15:30  ✅待上课
4. 第3节课  2026-07-09 14:00-15:30  ✅待上课
5. 第4节课  2026-07-16 14:00-15:30  ✅待上课
```
*说明：课程管理的排序为已结课置底，但每节课的序号固定不变，第1节课永远是"第1节课"。

**进入评价管理**：点击"评价管理"按钮，跳转至该课程的**评价管理**页面（详见 4.5 节）。

**截图指引**：截图展示打开课程管理弹窗的状态，标注：① 弹窗整体、② 已结课行（灰色删除线）、③ 待上课行（绿色标签）。

### 4.2 课程详情页

在我的课程页面中，**点击课程卡片本身**（不是按钮），进入该课程详情页。

#### 4.2.1 页面布局

**课程详情页结构**：
```
┌─────────────────────────────────────────────────┐
│ ← 返回         课程名称            右上角操作栏    │
├─────────────────────────────────────────────────┤
│ 首页 | 课程管理 | 学员管理 | 评论管理 | 成绩管理    │
│ 文件管理 | 作业管理                               │
├─────────────────────────────────────────────────┤
│                                                  │
│               Tab 内容区域                         │
│                                                  │
└─────────────────────────────────────────────────┘
```

- **顶部导航条**：左侧"← 返回"链接可回到"我的课程"列表
- **Tab 切换栏**：横排标签，点击切换不同功能
- **开课时间信息**：在首页 Tab 中展示，与课程管理弹窗中的时间一致

**截图指引**：截图展示课程详情页首页 Tab 的整体布局，标注：① 返回按钮、② 课程名称、③ Tab 切换栏、④ 开课时间信息。

#### 4.2.2 首页 Tab（课程信息）

默认进入时显示"首页"Tab，展示内容包括：
- 课程基本信息卡片：课程名称、分类、课程时长
- 开课时间（与课程表一致）
- 课程简介描述

#### 4.2.3 课程管理 Tab

点击"课程管理"Tab，展示课程时间安排列表，与 4.1.2 节课程管理弹窗内容相同。

#### 4.2.4 学员管理 Tab

点击"学员管理"Tab，展示选课学员列表。

**界面元素说明**：
- **统计数字**：顶部显示总人数、各小组人数
- **搜索框**：可按姓名或学号搜索
- **分组查看**：可按班级或组别切换查看
- **学员表格**：姓名、学号、班级、分组、操作列

**分组操作**：
- **手动拖拽**：在学员列表中将学员行拖拽到右侧分组区域
- **自动分组**：点击"自动分组"按钮，按人数或组数分配

**注意**：一个课程仅支持**一个分组方案**，一旦确定分组不建议频繁调整。

**截图指引**：截图展示学员管理 Tab 页面，标注：① 搜索框、② 学员列表表格、③ 分组区域。

#### 4.2.5 评论管理 Tab（评价系统核心）

点击"评论管理"Tab，进入该课程的评价管理模块。**这是教学管理的核心功能，请仔细阅读。**

##### 初始状态 — 未配置评价方案时

如果当前课程还未配置过评价方案，页面显示提示文案和引导按钮：
- 提示文字："请先配置评价方案和权重"
- 两个按钮："配置评价方案"和"配置权重"

##### 第一步：配置评价方案

点击"配置评价方案"按钮，打开配置弹窗。

**弹窗选项说明**：
1. **评价模板**（下拉选择）
   - **全模板**：支持所有评价类型（自评、组内互评、组间互评、教师评价、导师评价）
   - **标准**：自评 + 组内互评 + 教师评价
   - **简易**：自评 + 教师评价
   - **项目制**：组内互评 + 组间互评 + 教师评价

2. **评价频率**（下拉选择）
   - **双周评**：每 2 次课为 1 个评价轮次（如 5 次课 → 3 个轮次：第1轮=第1-2次课，第2轮=第3-4次课，第3轮=第5次课）
   - **每单元**：每次课对应 1 个评价轮次（如 5 次课 → 5 个轮次）
   - **项目里程碑**：每 2 次课合并为 1 个评价轮次（与双周评类似）

3. **逾期处理规则**（下拉选择）
   - **历史平均分**：逾期未评的使用该学员已有评价的平均分
   - **记 0 分**：逾期未评直接计 0 分
   - **记满分**：逾期未评直接计 100 分
   - **不处理**：逾期后仍开放评价入口

4. **导师评价开关**（开关按钮）
   - 开启后，评价类型中增加"导师评价"
   - 关闭后，评价类型不含导师评价

**操作步骤**：
1. 选择合适的模板、频率、逾期规则，决定是否启用导师评价
2. 点击"保存配置"
3. 弹窗关闭，配置保存成功

**截图指引**：截图展示配置评价方案的弹窗，标注四个配置项的位置。

##### 第二步：配置权重

配置完成后，点击"配置权重"按钮。

**弹窗内容**：
- **平时成绩权重配置**（根据选择的评价模板动态显示）
  - 自评权重（%）
  - 组内互评权重（%）
  - 组间互评权重（%）
  - 教师评价权重（%）
  - 导师评价权重（%）— 仅开启导师评价时显示
  - *所有平时成绩权重之和必须等于 100%*

- **期中/期末权重配置**（默认隐藏，需展开）
  - 期中笔试权重（%）
  - 期中项目权重（%）
  - 期末笔试权重（%）
  - 期末项目权重（%）

**操作步骤**：
1. 填写各权重数值（可在输入框中直接输入数字）
2. 确认总和为 100%（页面会实时显示"已填 XX%，剩余 XX%"）
3. 点击"保存权重"
4. 弹窗关闭，权重保存成功

**注意**：配置完成后，权重自动锁定，**评价期间不可修改**。如需修改请先解锁。

##### 第三步：待评列表（正式评价操作）

配置完成后，页面展示评价操作主界面。

**页面结构**：
```
┌──────────────────────────────────────────────────────┐
│  ⏳ 第1轮评价（未开始）     📅 第1-2节课              │
│  ┌────────────────────────────────────────────────┐  │
│  │ 学员姓名 | 自评 | 互评 | 组间评 | 教师评 | 导师评  │  │
│  ├────────────────────────────────────────────────┤  │
│  │ 张三     │ 85  │  90 │  78   │  -    │  -    │  │  │
│  │ 李四     │ 92  │  88 │  -    │  86   │  -    │  │  │
│  │ 王五     │ 55⚠️│  86 │  84   │  82   │  -    │  │  │
│  └────────────────────────────────────────────────┘  │
│  [优秀] [良好] [中等] [及格] [不及格]    [{第1轮}提交] │
└──────────────────────────────────────────────────────┘
```

**轮次区域说明**：
- 顶部显示轮次编号（如"第1轮评价"）、轮次状态标签（未开始 / 进行中 / 已锁定）
- 时间范围说明（如"第1-2节课"）
- **时间保护**：如果当前时间未到该轮次对应的第一节课上课时间，显示"该轮次尚未到开启时间"，表格数据为空或显示"-"

**学员评价表格**：
- 每行一个学员
- 列代表不同的评价类型（自评、组内互评、组间互评、教师评价、导师评价）
- 已评分数直接显示数字；未评显示"-"
- 分数单元格可点击（进入该学员的详细评价弹窗）
- **异常标记**：自评分数与他人均分差异 > 20 分时，该自评单元格以**特殊颜色背景 + ⚠️图标**标记

**单个评价**：
1. 点击学员行中的任意分数单元格（显示"-"或已有数字）
2. 弹出评价弹窗，弹窗内容：学员姓名、各评价类型、评分滑条或输入框（0-100）
3. 填写分数后点击"保存"

**批量评价**：
1. 在每个分组表头行，有一个**全选复选框**（勾选该组所有未提交学员）
2. 也可逐个勾选学员行前的复选框
3. 勾选后，顶部的等级按钮变为可点击状态（优秀/良好/中等/及格/不及格）
4. 点击任一等级按钮，所有选中学员的当前轮次评分统一设为该等级区间中间值：
   - 优秀 → 95 分
   - 良好 → 85 分
   - 中等 → 75 分
   - 及格 → 65 分
   - 不及格 → 35 分
5. 评分自动填入表格，此时尚未保存
6. 点击轮次区域的"提交"按钮，评价结果正式生效

**注意**：
- 评价填写后必须点击"提交"才真正保存，学生端才能查看
- 提交后的评价不可再修改（显示为已锁定状态）

**截图指引**：分多张截图——① 评价配置完成后的待评列表页面；② 点击某个学员评分单元格弹出的评价弹窗；③ 勾选多个学员后点击等级按钮批量评分的状态；④ 异常检测标记（自评分数异常的学员）。

##### 评价系统时间说明

评价轮次与课程时间严格绑定：

| 时间状态 | 第1轮评价状态 | 第N轮评价状态 | 最终轮状态 |
|---------|------------|------------|-----------|
| 第一节课前 | ⏳ 未开始（按钮禁用） | ⏳ 未开始 | ⏳ 未开始 |
| 第一节课后 | ✅ 可评价 | ⏳ 未开始 | ⏳ 未开始 |
| 第N节课后（对应轮次开启） | 🔒 已锁定 | ✅ 可评价 | ✅ 可评价 |
| 所有课程结束后第3天 | — | — | 🔒 截止锁定 |

当前系统使用**模拟参考日期**统一控制，默认日期为 `2026-06-24`（第一节课前一天），因此所有课程的评价均未开始。

#### 4.2.6 成绩管理 Tab

点击"成绩管理"Tab，进入该课程的成绩管理。

**界面元素说明**：
- **考试管理**：
  - 顶部"添加考试/项目"按钮
  - 考试列表（考试名称、日期、操作）
- **成绩录入**：
  - 班级/分组筛选下拉框
  - 学员成绩表格（姓名、各考试得分、总分）
- **成绩分析**：
  - 统计数据卡片：最高分、最低分、平均分、及格率
  - 成绩分布图：按分数段的柱状图（<60、60-69、70-79、80-89、90-100）
- **导入导出**：
  - "下载模板"按钮：按班级下载 Excel 模板
  - "导入成绩"按钮：上传填写好的 Excel
  - "导出 Excel"按钮：导出当前课程成绩

**操作步骤——导入成绩**：
1. 先选择班级/分组
2. 点击"下载模板"下载 Excel 模板文件
3. 在本地打开模板，按要求填写学生成绩
4. 点击"导入成绩"选择填写好的 Excel 文件
5. 提示导入结果（成功/失败的行数）

**操作步骤——导出 Excel**：
1. 选择要导出的班级/分组
2. 点击"导出 Excel"
3. 浏览器自动下载 .xlsx 文件

**截图指引**：截图展示成绩管理 Tab，标注：① 考试列表、② 成绩表格、③ 统计卡片、④ 成绩分布图。

#### 4.2.7 文件管理 Tab

点击"文件管理"Tab，进入课程云盘。

**界面元素说明**：
- "上传文件"按钮（右上角）
- 文件列表（文件名、大小、上传时间、操作列）
- 文件操作：预览、下载、删除
- 文件夹分类

**操作步骤**：
1. 点击"上传文件"按钮
2. 在文件选择弹窗中选择本地文件
3. 自动上传并显示在文件列表中
4. 点击文件的"下载"按钮可下载到本地

#### 4.2.8 作业管理 Tab

点击"作业管理"Tab，进入作业管理模块。

**界面元素说明**：
- "布置作业"按钮（右上角）
- 作业列表（作业标题、截止时间、提交数、操作）
- 作业详情弹窗（点击作业进入）：学生提交列表、批改入口

**布置作业操作**：
1. 点击"布置作业"按钮
2. 填写作业标题、描述文字、截止时间
3. 点击"发布"

**批改作业操作**：
1. 在作业列表中点击目标作业
2. 在作业详情弹窗中查看学生提交列表
3. 点击学生的"批改"按钮
4. 填写分数和评语
5. 点击"提交批改"

### 4.3 课程表页面

点击侧边栏"教学管理 > 课程表"进入。

**界面元素说明**：
- **顶部控制栏**：
  - "← 上一周"按钮
  - "返回本周"按钮（蓝色高亮）
  - "下一周 →"按钮
  - 显示当前周范围（如"2026年 第26周（06.22 - 06.28）"）
- **周课表视图**（类似学校课程表，7 列 × N 行）：
  - 每一列代表周一至周日
  - 每行代表一个时间段
  - 课程以彩色块展示，同一课程颜色相同
  - 课程块内显示：课程名称

**操作步骤**：
1. 使用"← 上一周"或"下一周 →"切换周次
2. 点击"返回本周"快速回到当前周
3. 浏览课程安排（彩色块区分不同课程）

**截图指引**：截图展示课程表周视图，标注：① 周次切换按钮、② 当前周范围、③ 课程颜色块。

### 4.4 成绩查询页面

点击侧边栏"教学管理 > 成绩查询"进入。

**界面元素说明**：
- **筛选区域**：
  - 课程筛选下拉框
  - 班级/分组筛选下拉框
  - 学员姓名搜索框（输入关键字自动匹配）
- **统计卡片**（选择课程后显示）：
  - 平均成绩（蓝色卡片）
  - 最高分（蓝色卡片）
  - 最低分（蓝色卡片）
  - 及格率（蓝色卡片）
- **成绩分布图**：按分数段柱状图
- **知识掌握热力图**：各知识点掌握程度（颜色深浅 = 掌握度高低）
- **成绩表格**：姓名、各考试分数、总分、操作列
- **右上角按钮**："导出 Excel"和"导出成绩"两个按钮

**导出成绩操作**：
1. 选择课程和班级/分组
2. 点击"导出成绩"按钮（蓝色）
3. 按钮变为**加载中状态**（旋转动画），持续 1 秒
4. 弹出提示弹窗：
   ```
   成绩数据导出成功，数据支持同步至教务系统
   ```
5. 点击"确认"关闭提示

**查看成绩详情**：
1. 在成绩表格中点击学员行的"查看详情"链接
2. 弹出成绩明细弹窗，展示：
   - 平时成绩各分项（自评、互评、教师评等）及权重
   - 大考成绩（期中/期末笔试、项目）
   - 最终成绩计算公式
   - 教师评语

**截图指引**：截图展示成绩查询页面，标注：① 筛选区域、② 统计卡片、③ 成绩分布图、④ 导出成绩的流程（点击→加载→弹窗提示）。

### 4.5 评价管理页面（独立页面）

点击侧边栏"教学管理 > 评价管理"进入，功能与课程详情内的评价管理相同，但展示更集中。

**界面元素说明**：
- **课程选择**：顶部课程筛选下拉框
- **Tab 切换**：与课程详情页内的评论管理 Tab 内容一致
- 配置评价方案和权重后，直接进入待评列表操作（同 4.2.5 节）

### 4.6 额外功能页面

点击侧边栏"教学管理 > 额外功能"进入。

**界面元素说明**：
- **评价待办提醒**：卡片列表展示未完成的评价任务（课程名称、轮次、未评学员数）
- **配置提醒**：未完成权重/评价方案配置的课程列表
- **待办事项**：个人待办 CRUD（增删改查）+ 勾选完成
- **云盘文件**：个人文件管理（上传/下载/删除）
- **在线文档**：在线文档 CRUD
- **个人笔记**：笔记管理（新建/编辑/删除）

---

## 五、学生端

登录角色选择 **"学生"**，推荐使用 `S2024001`（张明）账号。

侧边栏菜单项：个人画像、我的课表、我的课程、成绩查询、学习进度、额外功能。

### 5.1 个人画像页面

点击侧边栏"个人画像"进入。这是学生端最核心的数据可视化页面。

#### 5.1.1 页面布局

```
┌─────────────────────────────────────────────────┐
│   个人画像                                        │
│   查看个人信息与学习能力分析                        │
├─────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────┐ │
│ │  [头像]  张明                                │ │
│ │          邮箱：zhangming@edu.cn              │ │
│ │          电话：138****1234                   │ │
│ │          入学时间：2024-09-01                │ │
│ │          学号：S2024001                      │ │
│ │                                            │ │
│ │          已报名：5  已完成：3  平均分：85      │ │
│ └─────────────────────────────────────────────┘ │
│ ┌──────────────┐  ┌──────────────────────────┐ │
│ │  课程成绩雷达图│  │ 学习统计                  │ │
│ │  [雷达图 SVG] │  │ [统计图表]                │ │
│ └──────────────┘  └──────────────────────────┘ │
│ 能力分析：您的平均能力得分为 XX 分...            │
│ 职业推荐：                                     │
│ [AI应用开发工程师] [数据可视化工程师] [全栈AI]   │
└─────────────────────────────────────────────────┘
```

**个人信息卡片**：
- 左侧：圆形头像（显示姓名首字，背景蓝色）
- 右侧：姓名、邮箱、电话、入学时间、学号
- 最右侧：三个统计数字（已报名、已完成、平均分）

#### 5.1.2 能力雷达图

位于第二行左侧卡片。雷达图以多边形形式展示学生在各课程的评价成绩。

**雷达图说明**：
- 每个角代表一门课程
- 多边形面积越大，表示学生在该课程的评价成绩越好
- 五边形网格（同心多边形）用于参考刻度
- 右上角显示"点击查看详情"遮罩层
- **课程未开课时不显示该课程维度**

**交互操作**：
- 点击雷达图区域，弹出详情弹窗
- 弹窗内容：各课程得分明细、能力分析文字

**职业推荐**：根据各课程得分，系统自动推荐适合的职业方向：
- 以卡片形式展示，每张卡片包含：职业名称、描述文字、标签、匹配度评分
- 按匹配度从高到低排序，最多展示 3 个

**截图指引**：截图展示个人画像页面全貌，标注：① 个人信息卡片、② 能力雷达图、③ 职业推荐卡片。

### 5.2 我的课表页面

与教师端课程表页面布局相同，显示该学生已报名课程的时间安排。

**界面元素说明**（同 4.3 节）：
- 周次切换（上一周 / 下一周 / 返回本周）
- 彩色课程块
- 同课程同色

### 5.3 我的课程页面

点击侧边栏"我的课程"进入，展示学生已报名的课程列表。

**界面元素说明**：
- **页面标题**："我的课程"
- **课程卡片列表**（每个卡片）：
  - 课程名称（如"React 前端开发实战"）
  - 授课教师（如"王老师"）
  - 学习进度条（百分比显示已上课节数占比）
  - 右上角：AI 分层测试状态徽章（基础层 / 进阶层 / 卓越层），未测则无徽章
  - 右上角：待办评价红点角标（有未完成的评价时显示）
  - 卡片底部："去学习"按钮（蓝色，点击进入课程学习详情页）

**AI 分层测试说明**：
- 第一节课结束后自动开放 AI 分层测试
- 测试结果：基础层 / 进阶层 / 卓越层
- 以徽章形式展示在课程卡片右上角
- 逾期未测自动分配到基础层

**截图指引**：截图展示我的课程页面，标注：① 课程卡片、② 进度条、③ 分层徽章、④ 去学习按钮。

### 5.4 课程学习详情页

在我的课程页面中，点击课程卡片的"去学习"按钮进入。

#### 5.4.1 页面布局

```
┌─────────────────────────────────────────────────┐
│ ← 返回  课程名称                             进度:XX% │
├──────┬──────┬──────┬──────┬──────┬──────┐       │
│ 课程通知│ 课件资料│ 作业 │ 评价填写│综合评价│ 云盘  │
├──────┴──────┴──────┴──────┴──────┴──────┤       │
│                                            │
│          当前 Tab 内容区域                    │
│                                            │
└─────────────────────────────────────────────┘
```

#### 5.4.2 课程通知 Tab

展示教师发布的课程通知列表，每条通知显示标题、发布时间、内容摘要。

#### 5.4.3 课件资料 Tab

展示教师上传的课件资料列表，支持在线预览和下载。

#### 5.4.4 作业 Tab

**界面元素说明**：
- **作业列表**：作业标题、截止时间、状态（待提交 / 已提交 / 已批改）、得分
- **提交作业**：点击未提交的作业，弹出上传弹窗，选择文件后提交

#### 5.4.5 评价填写 Tab

这是学生端最常用的评价入口。

**界面元素说明**：
- **轮次列表**：每个评价轮次显示为一张卡片
- **卡片内容**：
  - 轮次名称（如"第1轮评价"）
  - 轮次状态标签：
    - ⏳ **未开始**：灰色文字"第一节课尚未开始"标签，按钮禁用不可点击
    - ✅ **进行中**：蓝色标签，显示"已评 X/Y 项"，按钮可点击
    - 🔒 **已锁定**：灰色标签"已锁定"，按钮不可点击
    - ⚠️ **已逾期**：红色边框突出显示，按钮可点击但显示"已逾期"标签
  - "去评价"按钮（蓝色，仅进行中/已逾期状态可点击）

**评价填写弹窗**（点击"去评价"按钮弹出）：
- 弹窗标题：轮次名称
- 评价类型列表（根据教师配置的方案）
  - 自评：对自己学习表现评分（0-100 分）
  - 组内互评：对同组同学评分
  - 组间互评：对其他组评分
  - 教师评价：对教师教学评分
  - 导师评价：对导师指导评分
- 每个评价类型：名称 + 输入框（手动输入 0-100 的整数）
- 底部 "提交评价" 按钮：提交后显示"已评 N/N 项"

**注意**：
- 第一节课前按钮禁用，显示"第一节课尚未开始"
- 已提交的评价显示分数，但不可修改（重新显示为已锁定）

**截图指引**：截取评价填写 Tab，标注：① 不同状态的轮次卡片、② 进行中轮次的已评进度、③ 评价填写弹窗。

#### 5.4.6 综合评价 Tab

**界面元素说明**：
- 课程未开课时：显示"暂无评价数据"
- 课程开课后：显示各评价维度的得分条（自评得分、互评得分等）
- 总成绩（最终加权平均分）
- 班级平均分（同课程其他学员的平均分）

#### 5.4.7 云盘 Tab

查看课程共享的云盘文件，支持下载（只读视图，学生不可上传）。

### 5.5 成绩查询页面

点击侧边栏"成绩查询"进入。

**界面元素说明**：
- **统计卡片**（四张蓝色卡片依次排列）：
  - 平均成绩
  - 最高分
  - 最低分
  - 已评课程数
- **成绩列表**：每个课程一张成绩卡片
  - 左侧：课程名称、授课教师
  - 右侧：总分 + 评分等级徽章（优秀 / 良好 / 中等 / 及格 / 不及格）
  - 底部："查看详情"按钮

**评分等级对照**：
| 分数范围 | 等级 |
|---------|------|
| ≥ 90 分 | 优秀（绿色徽章）|
| ≥ 80 分 | 良好（蓝色徽章）|
| ≥ 70 分 | 中等（灰色徽章）|
| ≥ 60 分 | 及格（灰色徽章）|
| < 60 分 | 不及格（红色徽章）|

**查看成绩明细**：
1. 点击课程成绩卡片上的"查看详情"按钮
2. 弹出成绩明细弹窗：
   - **平时成绩构成**：逐项展示自评、互评、组间评、教师评、导师评的分数和权重
   - **大考成绩**：期中/期末笔试、项目得分
   - **最终成绩计算公式**：平时成绩 × 权重 + 大考成绩 × 权重
   - **教师评语**：教师填写的主观评价文字
3. **课程未开课时，不显示分项成绩明细**（"查看详情"按钮不可点击或弹窗显示暂无数据）

**截图指引**：截图展示成绩查询页面，标注：① 统计卡片、② 课程成绩列表（含等级徽章）、③ 成绩明细弹窗。

### 5.6 学习进度页面

点击侧边栏"学习进度"进入。

**界面元素说明**：
- **课程进度列表**：每个课程一行
- **进度条**：蓝色进度条显示已上课节数占比（如 3/5 = 60%）
- **课程未开课时进度 = 0%**

### 5.7 额外功能页面

同教师端 4.6 节，提供待办事项、云盘文件、在线文档、个人笔记功能。

---

## 六、企业导师端

登录角色选择 **"企业导师"**，账号 `mentor-zhang`，密码 `666666`。

侧边栏菜单：我的课程、课程表、成绩查询、额外功能。

### 6.1 我的课程页面

与教师端课程列表布局相同，展示作为导师负责的课程。

### 6.2 课程详情页面

点击课程卡片进入。

**导师端特有功能——评价 Tab**：
- 课程详情页内包含「评价」Tab（导师专用的评价入口）
- **评价 Tab 状态说明**：
  - 第一节课未开始时：显示提示文字"第一节课尚未开始，评价暂未开放"，评价按钮不可点击
  - 评价开启后：显示学员列表，每行一个学员，右侧为评分输入框（0-100 分）
  - 已评分学员：输入框中显示已填分数，右侧显示"已评分"文字
- **评价操作**：
  1. 在学员行的评分输入框中填写分数
  2. 点击底部的"提交评价"按钮
  3. 提交后该学员行显示分数和"已评分"状态

**其他 Tab**：
- 首页、课程管理、学员管理（只读视图）：仅查看不可编辑
- 文件管理（只读视图）
- 作业管理（只读视图）

### 6.3 课程表和成绩查询

与教师端视图相同，仅查看不可编辑。

### 6.4 额外功能

同学生端 5.7 节。

---

## 七、学院领导端

登录角色选择 **"学院领导"**，推荐使用 `leader-liu`（刘院长）账号。

### 7.1 双重身份说明

部分领导账号可同时兼任教师或导师：

| 账号 | 领导身份 | 兼任角色 | 侧边栏组合 |
|------|---------|---------|-----------|
| `leader-zhou` | 普通领导 | 无 | 只显示「学院管理」|
| `leader-liu` | 领导 | **教师** | 学院管理 + **教学管理**（同教师端）|
| `leader-zheng` | 领导 | **教师** | 学院管理 + **教学管理**（同教师端）|
| `leader-wu` | 领导 | **导师** | 学院管理 + **导师端菜单**|

### 7.2 学院管理页面

#### 7.2.1 课程总览

**界面元素说明**：
- **搜索框**：按课程名称搜索
- **分类分组**：按分类分组展示课程（只读）
- **管辖范围**：领导只可查看自己管辖分类下的课程
  - 刘院长：编程开发、数据科学
  - 周院长：全部分类
  - 郑院长：编程开发
  - 吴院长：全部分类

**操作**：
1. 在分类分组中浏览课程
2. 点击课程卡片可进入课程详情页（视图与教师端详情页一致，部分只读）

#### 7.2.2 学员总览

**界面元素说明**：
- **统计卡片**：顶部显示总学员数
- **搜索框**：按姓名或学号搜索
- **课程筛选**：按下拉筛选查看特定课程的学员
- **学员列表**：姓名、学号、课程数、操作

### 7.3 兼任角色功能

具有教师身份的学院领导（刘院长、郑院长），侧边栏中会显示"教学管理"菜单，功能与教师端完全一致（见第四章）。

具有导师身份的学院领导（吴院长），侧边栏会显示导师端菜单，功能与企业导师端一致（见第六章）。

---

## 八、评价系统原理说明

### 8.1 评价轮次划分

评价按**轮次**（Session）组织，每轮对应一次或多次课：

| 频率模式 | 划分规则 | 举例：5次课 |
|---------|---------|-----------|
| **双周评** | 每 2 次课为 1 轮 | 第1轮：1-2次课，第2轮：3-4次课，第3轮：5次课 |
| **每单元** | 每次课为 1 轮 | 共 5 轮 |
| **项目里程碑** | 每 2 次课为 1 轮（最后一轮可能是 1 次课） | 共 3 轮 |

### 8.2 评价时间窗

- 第 1 轮：第一节课**上课后**开启
- 第 N 轮：第 N 轮对应课次的**第一节课上课后**开启
- 最终轮：最后一节课结束后 **3 天**自动截止

### 8.3 评价状态流转

```
┌─────────────┐
│  ⏳ 未开始   │ ← 第一节课前 / 该轮次时间未到
└──────┬──────┘
       ↓ 对应课次上课后
┌─────────────┐
│  ✅ 进行中    │ ← 可填写评价，教师未提交前可修改
└──────┬──────┘
       ↓ 教师点击"提交"或下一轮开启
┌─────────────┐
│  🔒 已锁定    │ ← 不可修改，学员端可查看结果
└─────────────┘
```

### 8.4 异常检测规则

**检测条件**：自评分数 - 其他人（组内互评、组间互评、教师评价、导师评价）平均分 > 20 分。

**展示方式**：教师端评价表格中，异常的自评单元格以**黄色/红色背景**突出显示，并附加 ⚠️ 图标。

**作用**：帮助教师快速发现学生自评与实际表现偏差过大。

### 8.5 全局模拟参考日期

> 本系统使用模拟时间机制，用于开发和演示测试。后端对接实际教务系统后，此机制将被真实时间替换。

- **当前模拟日期**：`2026-06-24`
- **设定原因**：所有课程第一节课在 `2026-06-25`，在此日期之前可观看到"未开课"状态的完整界面
- **影响范围**：
  - 所有课程的"是否已开课"判定
  - 所有评价轮次的"是否已开启"判定
  - 课程进度的百分比计算
  - 成绩明细的展示/隐藏

---

## 九、常见问题

**Q1：登录后页面空白或菜单加载不全？**

A：检查登录时选择的角色是否正确。例如 `teacher-wang` 必须选择"教师"，`mentor-zhang` 必须选择"企业导师"。

**Q2：如何重置为初始演示数据？**

A：打开浏览器开发者工具（F12），按以下步骤操作：
1. 切换到 Application（应用）标签
2. 左侧选择 Storage > Local Storage（本地存储）
3. 右键点击 http://localhost:5173 下的条目
4. 选择 Clear（清除）
5. 刷新页面即可恢复初始数据

**Q3：修改了数据，刷新后丢失了？**

A：系统数据自动保存在浏览器 localStorage 中，刷新页面不会丢失。如果丢失了，说明可能不小心清除了浏览器数据。

**Q4：为什么课程全部显示"未开始"？**

A：因为系统使用模拟参考日期 `2026-06-24`，该日期在所有课程第一节课日期之前。这是用于展示"开课前"状态的演示用途。实际部署后系统将使用真实日期。

**Q5：为什么评价按钮是灰色的点不了？**

A：有两个可能原因：
1. 当前时间未到该轮次的开启时间（各端均是如此）
2. 该轮次已锁定（上一轮的评价）

**Q6：如何批量给多名学生打分？**

A：在教师端评价管理页面：
1. 在评价表格中找到「全选」复选框（每个分组一个，独立控制）
2. 或手动勾选需要评分的学生
3. 点击顶部的等级按钮（优秀/良好/中等/及格/不及格）
4. 系统自动为选中学生统一赋值对应等级分数
5. 最后点击"提交"保存

**Q7：为什么我改了评价，学生端看到的是旧的？**

A：教师提交评价后学生端才能看到最新结果。在评价表格中点击"提交"按钮后，学生端刷新页面即可查看。

**Q8：学院领导能编辑课程吗？**

A：普通领导（如周院长）只能查看，无权编辑。具有教师身份的学院领导（如刘院长、郑院长）可以编辑自己教授的课程。

**Q9：导师能像教师一样管理课程吗？**

A：导师端以**只读为主**，可以查看课程信息、学员分组、成绩等，但不可编辑。导师的主要操作是为学员打分（在评价 Tab 中）。

**Q10：为什么有的领导能看到"教学管理"菜单？**

A：这些领导同时兼任教师或导师角色，平台自动合并了对应菜单。详情见 7.1 节的账号说明。

**Q11：添加考试/项目后在哪里查看？**

A：在课程详情 > 成绩管理 Tab 中，添加考试/项目后会在成绩录入区域显示为列字段，填写的成绩会对应显示在成绩表中。
